# 更新日志

本文件记录面向使用者的变更。内部重构见各模块的 git 历史。

## 1.1.0

### 切台协议重构

- `next_shot` + `confirm_switch` 合并为单一的 `shot_state`，每次切台都同时上报
  `current`（当前播送）与 `next`（即将切台）。
- 接收端不再需要自己推断「正在播送」，解说端也没有了本地过期计时器。
- 导播端新增「确认已切」按钮，形成「预告 → 确认」两步闭环。
- 解说端界面改为单块显示区，按后端指令在「即将播送 / 正在播送」之间切换。

> ⚠️ **不兼容变更**：旧客户端的 `next_shot` / `confirm_switch` 会被后端拒绝并回一条
> `system` 错误。**各端客户端需要一起升级。**

### 安全

- 修复**任意人可注册管理员账号**的严重漏洞。`POST /api/auth/register` 原本挂在公开
  路由且不校验 `role`，任何能访问 3000 端口的人都能直接开管理员并调用全部管理接口。
  现在：用户表为空时第一个注册的人自动成为管理员；之后该接口收紧为仅管理员可用。
- 管理接口新增角色校验（`RequireRole("admin")`）。此前任何登录用户（包括最低权限的
  导播）都能列出全部用户与项目、增删项目、分配权限。
- 角色每次查库、不信任令牌内容，所以后台降权**立即生效**，不用等 60 分钟令牌过期。
- 修复未授权响应返回 `400` + 空 body 的问题（应返回带可读消息的 401/403）。
- 用户名增加字符白名单与长度限制，密码最少 6 位。

> ⚠️ 全新部署请在对外开放端口前创建首个管理员，否则可能被他人抢先注册。

### 心跳不再计入消息

- 心跳改为在入库**之前**丢弃：不写 `messages` 表、不计入项目消息统计、不转发给其他端。
- 新增迁移 `20260926000001_purge_heartbeat_messages` 清理历史存量
  （1401 条记录里有 1251 条是心跳）。执行 `smart-mzcmc migrate` 即可，不可逆。

### 界面

- 管理后台与后端首页支持跟随系统的浅色/深色外观，纯 CSS 实现，无 JS、无首屏闪烁。
- 管理后台插件页改为显示插件的**真实启用状态**、停用原因与生效配置（原先一律显示
  「已启用」），机密值脱敏。
- 日志与首页动态改为渲染可读摘要，不再直接显示原始 JSON。
- 新增数据库迁移入口 `smart-mzcmc migrate`（此前无任何方式执行迁移）。

### 部署

- 新增 `backend/tools/package.go`：交叉编译 linux/amd64 并打发布包，自带产物校验。
- 发布包自带 `start.sh`、systemd 单元与 `DEPLOY.md`。
- 支持反向代理部署（单域名收拢 3000 / 3002），系统首页自动识别并显示对应地址。
- 采访端 Web 修复两处「构建通过、线上白屏」：补 `--base-href /interviewer/` 与
  `--no-web-resources-cdn`（后者解决内网无外网时 CanvasKit 拉不到的问题）。
- 采访端配置改为**运行期可改**（`public/interviewer/config.json`），换服务器地址
  不必重新构建。

### 运维

- 静态 HTML 补 `Cache-Control`，避免改完首页浏览器仍显示旧内容。
- 工作目录错误时 `/admin`、`/docs` 返回带说明的 404，而非空响应。
- 发布包不会包含 `database/`、`storage/` 下的运行期数据（有主动拦截）。
- 自动创建运行期目录，首次部署不需要手工 `mkdir`。

## 1.0.0

- 首个正式版本：管理后台、导播端、解说端、包装端、采访端，控制权互斥与切台联动。

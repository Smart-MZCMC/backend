# 文档站由 `cd docs && pnpm run build:deploy` 生成，产物不入库。
docs/

# 管理端与采访端产物随发布包分发，但同样不入库：
#   admin/      由 Smart-MZCMC/admin 构建（CI 跨仓库拉取后拷贝到 public/admin）
#   interviewer/由 Smart-MZCMC/interviewer 构建（CI 跨仓库拉取后 flutter build web）
# 注意：这两个目录下已有的历史产物仍被 git 跟踪，新增/删除文件不会被本文件忽略，
# 需要清理历史产物时用 `git rm -r --cached public/admin public/interviewer`。
